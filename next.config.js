/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com', 'thumbs.dreamstime.com','images.unsplash.com'],
    }
}

module.exports = nextConfig
